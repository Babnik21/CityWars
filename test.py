from random import shuffle, sample
from math import ceil, floor
import values

class Aa():
    def __init__(self, lst):
        self.lst = lst

x = {1: "goo", 2: "Drek"}
lst = list(x.values())
print(lst)
